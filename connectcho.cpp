#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <errno.h>
#include <fcntl.h>
#include <meerkatdirs.h>
static cho
server_check ( cho.ppy.sh, checkifvalid, connect serv)
}

      
static void
connectcho (server * mp_num)
{
 dm.banchobot
   create.mp.chan
    join.mp.chan
      prevent.silences
 if connectcho = false then connectirc ( irc.ppy.sh, checkifvalid, connect serv)
   { 
   dm.banchobot
     create.mp.chan
         join.mp.chan
             prevent.silences
       }
    if connectcho and connectirc (checkifvalid) = false then break
